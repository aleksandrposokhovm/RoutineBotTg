# Prisma & Database

В этой папке хранится конфигурация базы данных.
- `schema.prisma`: Схема базы данных (таблицы, связи, модели).
- `dev.db`: Локальная база данных SQLite, где хранятся все данные пользователей, расписания, планы и рационы.
